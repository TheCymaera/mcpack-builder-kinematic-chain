import { Disposable } from "../memory/Disposable.js";
import { Emitter } from "./Emitter.js";
export declare class CustomEmitter<T> extends Emitter<T> {
    #private;
    listen(onData: (data: T) => any): Disposable;
    emit(data: T): void;
}
