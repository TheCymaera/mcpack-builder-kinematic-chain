export declare class Keyframes<T> {
    #private;
    constructor(frames: Keyframes.Frame<T>[]);
    step(deltaTime: number): void;
    stepAndLoop(deltaTime: number): void;
    getCurrentFrame(): T;
}
export declare namespace Keyframes {
    interface Frame<T> {
        duration: number;
        value: T;
    }
    interface InterpolatedFrame<T> {
        value1: T;
        value2: T;
        lerp: number;
    }
}
