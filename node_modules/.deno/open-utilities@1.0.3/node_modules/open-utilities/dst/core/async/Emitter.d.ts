import { Disposable } from "../memory/Disposable.js";
export declare abstract class Emitter<T> {
    abstract listen(onData: (data: T) => any, options?: {
        onClose?: () => any;
    }): Disposable;
    single(): Promise<T>;
    [Symbol.asyncIterator](): any;
}
