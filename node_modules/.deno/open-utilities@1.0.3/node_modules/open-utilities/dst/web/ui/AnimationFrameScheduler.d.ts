import { Duration } from "../../core/datetime/Duration.js";
export declare class AnimationFrameScheduler {
    #private;
    private constructor();
    dispose(): void;
    static schedule(): Promise<Duration>;
    static periodic(callback: (elapsedTime: Duration) => any): AnimationFrameScheduler;
}
